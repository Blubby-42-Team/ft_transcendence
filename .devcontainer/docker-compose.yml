services:
  blog:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workplaces:cached
    ports:
      - 8080:8080
    command: /bin/sh -c "while sleep 1000; do :; done"
    networks:
      - dev

networks:
  dev:
